# Secrets — never commit. .env.example is the committed template.
.env

# Build artifacts
/bin/
/backups/
# WHY the leading slash: a bare "corridord" also matches cmd/corridord/,
# which silently kept the entire main package out of git — the fix(kalshi)
# config change never shipped because of it. Anchored = binary only.
/corridord
